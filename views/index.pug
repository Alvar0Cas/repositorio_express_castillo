extends layout

block content
  h1 "StrideSpike"
  div.logo2 
  img.logo2_img(src='/images/logo2.png', alt='logo.png')
  p Bienvenido a nuestra página de ventas de TENIS.

  form(action='/buscar-producto', method='GET' class='search-bar')
    input(type='text', name='q', placeholder='Buscar producto...')
    button(type='submit') Buscar